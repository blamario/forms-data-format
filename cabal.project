packages: .

-- Some transitive dependencies have overly strict upper bounds on `base`
-- that exclude GHC 9.6+.  This flag relaxes those bounds globally so the
-- package can be built with a modern GHC toolchain.
allow-newer: all:base
